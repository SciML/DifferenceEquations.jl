var documenterSearchIndex = {"docs":
[{"location":"basics/problem_types/#Problem-Types","page":"Problem Types","title":"Problem Types","text":"DifferenceEquations.jl provides a hierarchy of problem types for defining discrete-time state-space models. All concrete problem types inherit from AbstractStateSpaceProblem and share a common interface for specifying dynamics, observations, and noise.","category":"section"},{"location":"basics/problem_types/#Abstract-Type","page":"Problem Types","title":"Abstract Type","text":"","category":"section"},{"location":"basics/problem_types/#LinearStateSpaceProblem","page":"Problem Types","title":"LinearStateSpaceProblem","text":"","category":"section"},{"location":"basics/problem_types/#QuadraticStateSpaceProblem","page":"Problem Types","title":"QuadraticStateSpaceProblem","text":"","category":"section"},{"location":"basics/problem_types/#PrunedQuadraticStateSpaceProblem","page":"Problem Types","title":"PrunedQuadraticStateSpaceProblem","text":"","category":"section"},{"location":"basics/problem_types/#StateSpaceProblem","page":"Problem Types","title":"StateSpaceProblem","text":"","category":"section"},{"location":"basics/problem_types/#Common-Keyword-Arguments","page":"Problem Types","title":"Common Keyword Arguments","text":"The following keywords are shared by all problem constructors:\n\nKeyword Description Default\nobservables_noise Observation noise covariance matrix (AbstractMatrix, e.g. Diagonal(d) or Symmetric(H * H')) nothing\nobservables Observed data as Vector{Vector{T}} nothing\nnoise Fixed noise as Vector{Vector{T}} nothing (drawn randomly)\nsyms State variable names as a Tuple of Symbols, e.g. (:x, :y) nothing\nobs_syms Observation variable names as a Tuple of Symbols nothing","category":"section"},{"location":"basics/problem_types/#Linear-only-keywords","page":"Problem Types","title":"Linear-only keywords","text":"These are accepted only by LinearStateSpaceProblem:\n\nKeyword Description Default\nC Observation matrix nothing\nu0_prior_mean Prior mean for Kalman filtering nothing\nu0_prior_var Prior covariance for Kalman filtering nothing","category":"section"},{"location":"basics/problem_types/#Quadratic-only-keywords","page":"Problem Types","title":"Quadratic-only keywords","text":"QuadraticStateSpaceProblem and PrunedQuadraticStateSpaceProblem accept C_0, C_1, C_2 instead of C.","category":"section"},{"location":"basics/problem_types/#Generic-only-keywords","page":"Problem Types","title":"Generic-only keywords","text":"StateSpaceProblem requires the additional positional/keyword arguments n_shocks and n_obs to specify dimensions.","category":"section"},{"location":"basics/problem_types/#Dual-role-of-observables_noise","page":"Problem Types","title":"Dual role of observables_noise","text":"The observables_noise keyword has a dual role:\n\nDuring simulation (when observables is not provided): observation noise with this covariance is added to the simulated observations sol.z.\nDuring likelihood computation (when observables is provided): it defines the observation noise covariance used in the log-likelihood calculation.\n\nnote: Note\nobservables_noise must be an AbstractMatrix. For diagonal noise, use Diagonal([σ₁², σ₂², …]) where the entries are variances (not standard deviations). For a general covariance, use a full Matrix or Symmetric(H * H').","category":"section"},{"location":"basics/problem_types/#Remaking-Problems","page":"Problem Types","title":"Remaking Problems","text":"Use remake to create a modified copy of a problem, changing specific fields while keeping everything else. This is useful for parameter sweeps and optimization loops.\n\nusing DifferenceEquations, LinearAlgebra\nA = [0.95 6.2; 0.0 0.2]\nB = [0.0; 0.01;;]\nprob = LinearStateSpaceProblem(A, B, zeros(2), (0, 5))\nprob2 = remake(prob; u0 = [0.1, 0.2])\nsol2 = solve(prob2)\nsol2.u[1]  # new initial condition","category":"section"},{"location":"basics/problem_types/#DifferenceEquations.AbstractStateSpaceProblem","page":"Problem Types","title":"DifferenceEquations.AbstractStateSpaceProblem","text":"AbstractStateSpaceProblem <: DEProblem\n\nAbstract supertype for all discrete-time state-space problems in DifferenceEquations.jl.\n\nSubtypes include LinearStateSpaceProblem, QuadraticStateSpaceProblem, PrunedQuadraticStateSpaceProblem, and StateSpaceProblem.\n\n\n\n\n\n","category":"type"},{"location":"basics/problem_types/#DifferenceEquations.LinearStateSpaceProblem","page":"Problem Types","title":"DifferenceEquations.LinearStateSpaceProblem","text":"LinearStateSpaceProblem(A, B, u0, tspan[, p]; kwargs...)\n\nDefine a linear time-invariant state-space model:\n\nu_n+1 = A  u_n + B  w_n+1\n\nwith optional observation equation z_n = C  u_n + v_n.\n\nPositional Arguments\n\nA: Transition matrix (n×n).\nB: Noise input matrix (n×k), or nothing for deterministic dynamics.\nu0: Initial state vector, or a Distribution for random initial conditions.\ntspan: Time span as (t0, t_end) with integer distance (e.g., (0, T)).\np: Parameters (default: NullParameters()).\n\nKeyword Arguments\n\nC: Observation matrix (m×n). If nothing, no observations are computed.\nobservables_noise: Observation noise covariance matrix (AbstractMatrix, e.g. Diagonal(d) or Symmetric(H * H')).\nobservables: Observed data as Vector{Vector{T}} for likelihood computation.\nnoise: Fixed noise sequence as Vector{Vector{T}}. If nothing, noise is drawn randomly.\nu0_prior_mean: Prior mean for Kalman filtering.\nu0_prior_var: Prior covariance matrix for Kalman filtering.\nsyms: State variable names (e.g., (:x, :y)) for symbolic indexing.\nobs_syms: Observation variable names for symbolic indexing.\n\nNotes\n\nProviding u0_prior_mean, u0_prior_var, observables, and observables_noise (with noise = nothing) triggers automatic selection of KalmanFilter.\nThe observables timing convention: observations correspond to z_1 z_2 ldots (starting from the second state), so pass T observations for a tspan of (0, T).\n\nSee also: StateSpaceProblem, QuadraticStateSpaceProblem, DirectIteration, KalmanFilter.\n\n\n\n\n\n","category":"type"},{"location":"basics/problem_types/#DifferenceEquations.QuadraticStateSpaceProblem","page":"Problem Types","title":"DifferenceEquations.QuadraticStateSpaceProblem","text":"QuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, tspan[, p]; kwargs...)\n\nDefine a second-order (quadratic) state-space model:\n\nu_n+1 = A_0 + A_1  u_n + u_n^top A_2  u_n + B  w_n+1\n\nwith optional observation equation z_n = C_0 + C_1  u_n + u_n^top C_2  u_n + v_n.\n\nPositional Arguments\n\nA_0: Constant drift vector (length n).\nA_1: Linear transition matrix (n×n).\nA_2: Quadratic transition tensor (n×n×n). Entry A_2[i,:,:] gives the matrix for the i-th element of the quadratic term.\nB: Noise input matrix (n×k), or nothing.\nu0: Initial state vector.\ntspan: Time span as (t0, t_end).\n\nKeyword Arguments\n\nC_0, C_1, C_2: Observation equation coefficients (analogous to A_0, A_1, A_2).\nobservables_noise, observables, noise, syms, obs_syms: Same as LinearStateSpaceProblem.\n\nReferences\n\nAndreasen, Fernandez-Villaverde, and Rubio-Ramirez (2017), \"The Pruned State-Space System for Non-Linear DSGE Models: Theory and Empirical Applications.\"\n\nSee also: PrunedQuadraticStateSpaceProblem, LinearStateSpaceProblem.\n\n\n\n\n\n","category":"type"},{"location":"basics/problem_types/#DifferenceEquations.PrunedQuadraticStateSpaceProblem","page":"Problem Types","title":"DifferenceEquations.PrunedQuadraticStateSpaceProblem","text":"PrunedQuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, tspan[, p]; kwargs...)\n\nDefine a pruned second-order state-space model. Unlike QuadraticStateSpaceProblem, the quadratic terms operate on a separate linear-part state u_f rather than the full state:\n\nu_f^n+1 = A_1  u_f^n + B  w_n+1\n\nu_n+1 = A_0 + A_1  u_n + (u_f^n)^top A_2  u_f^n + B  w_n+1\n\nThe observation equation similarly uses u_f: z_n = C_0 + C_1  u_n + (u_f^n)^top C_2  u_f^n + v_n.\n\nThis pruning approach prevents explosive dynamics in higher-order perturbation solutions. Arguments are identical to QuadraticStateSpaceProblem.\n\nReferences\n\nAndreasen, Fernandez-Villaverde, and Rubio-Ramirez (2017), \"The Pruned State-Space System for Non-Linear DSGE Models: Theory and Empirical Applications.\"\n\nSee also: QuadraticStateSpaceProblem.\n\n\n\n\n\n","category":"type"},{"location":"basics/problem_types/#DifferenceEquations.StateSpaceProblem","page":"Problem Types","title":"DifferenceEquations.StateSpaceProblem","text":"StateSpaceProblem(transition, observation, u0, tspan[, p]; n_shocks, kwargs...)\n\nDefine a generic state-space model with user-provided callback functions:\n\nu_n+1 = f(u_n w_n+1 p t_n) quad z_n = g(u_n p t_n)\n\nPositional Arguments\n\ntransition: Callback f!!(x_next, x, w, p, t) -> x_next. For mutable arrays, mutate x_next in place and return it; for immutable arrays (e.g., SVector), return a new value.\nobservation: Callback g!!(y, x, p, t) -> y, or nothing for no observations.\nu0: Initial state vector, or a Distribution for random initial conditions.\ntspan: Time span as (t0, t_end) with integer distance.\np: Parameters passed to the callbacks (default: NullParameters()).\n\nKeyword Arguments\n\nn_shocks::Int: Number of noise dimensions (required).\nn_obs::Int: Number of observation dimensions (default: 0).\nobservables_noise: Observation noise covariance matrix (AbstractMatrix, e.g. Diagonal(d) or Symmetric(H * H')).\nobservables: Observed data as Vector{Vector{T}}.\nnoise: Fixed noise sequence as Vector{Vector{T}}.\nsyms: State variable names for symbolic indexing.\nobs_syms: Observation variable names for symbolic indexing.\n\nSee also: LinearStateSpaceProblem, DirectIteration.\n\n\n\n\n\n","category":"type"},{"location":"basics/workspace/#Workspace-API","page":"Workspace API","title":"Workspace API","text":"The workspace API provides a pre-allocated, reusable solving pattern via init and solve!. This avoids repeated memory allocation when solving the same type of problem many times, and is required for compatibility with Enzyme.jl reverse-mode AD.","category":"section"},{"location":"basics/workspace/#Creating-and-Using-a-Workspace","page":"Workspace API","title":"Creating and Using a Workspace","text":"","category":"section"},{"location":"basics/workspace/#Basic-Usage","page":"Workspace API","title":"Basic Usage","text":"using DifferenceEquations, LinearAlgebra, Random\nA = [0.95 6.2; 0.0 0.2]\nB = [0.0; 0.01;;]\nC = [0.09 0.67; 1.00 0.00]\nu0 = zeros(2)\nprob = LinearStateSpaceProblem(A, B, u0, (0, 5); C)\nws = init(prob, DirectIteration())\nsol = solve!(ws)\nsol.u[end]","category":"section"},{"location":"basics/workspace/#Cache-Reuse","page":"Workspace API","title":"Cache Reuse","text":"Calling solve!(ws) again on the same workspace reuses all previously allocated buffers. The solver fully overwrites all output arrays on each call, so no manual reset is needed between calls. This makes the workspace pattern ideal for tight loops:\n\nws = init(prob, DirectIteration())\nfor i in 1:1000\n    sol = solve!(ws)\n    # process sol...\nend\n\nYou can also change the problem between calls for parameter sweeps using remake:\n\nws = init(prob, DirectIteration())\nfor a11 in [0.9, 0.95, 1.0]\n    ws.prob = remake(ws.prob; A = [a11 6.2; 0.0 0.2])\n    sol = solve!(ws)\n    # process sol.logpdf...\nend","category":"section"},{"location":"basics/workspace/#Endpoints-Only-Mode-(save_everystepfalse)","page":"Workspace API","title":"Endpoints-Only Mode (save_everystep=false)","text":"Pass save_everystep=false to init to allocate minimal 2-element buffers. The solver stores only the initial and final states, while still correctly accumulating logpdf:\n\nws_ep = init(prob, DirectIteration(); save_everystep=false)\nsol_ep = solve!(ws_ep)\nlength(sol_ep.u)  # 2: [u_initial, u_final]\n\nThis is especially useful for ForwardDiff gradient computation, where reducing the number of dual-number allocations from O(T) to O(1) gives significant speedups (up to 7x with StaticArrays):\n\n# ForwardDiff benefits from save_everystep=false\nfunction neg_loglik(params)\n    prob = make_problem(params)\n    return -solve(prob, ConditionalLikelihood(); save_everystep=false).logpdf\nend\nForwardDiff.gradient(neg_loglik, params0)","category":"section"},{"location":"basics/workspace/#When-to-Use","page":"Workspace API","title":"When to Use","text":"The workspace API is useful in the following scenarios:\n\nEnzyme AD: Enzyme requires pre-allocated buffers passed as Duplicated arguments. The workspace pattern via init/solve! is the recommended way to use Enzyme with DifferenceEquations.jl. See Enzyme AD for details.\nRepeated solves in optimization loops: When solving the same problem structure many times (e.g., during parameter estimation), the workspace avoids allocating new arrays on every iteration.\nForwardDiff with save_everystep=false: Combining the workspace API with endpoints-only mode minimizes dual-number allocations, giving the best ForwardDiff performance.\nPerformance-critical code: Eliminating allocations reduces GC pressure and improves performance, especially for small to medium-sized problems.","category":"section"},{"location":"basics/workspace/#DifferenceEquations.StateSpaceWorkspace","page":"Workspace API","title":"DifferenceEquations.StateSpaceWorkspace","text":"StateSpaceWorkspace\n\nWorkspace for state-space problem solvers. Holds the problem, algorithm, pre-allocated output arrays, and scratch cache. Created by CommonSolve.init and consumed by CommonSolve.solve!.\n\n\n\n\n\n","category":"type"},{"location":"basics/workspace/#CommonSolve.init","page":"Workspace API","title":"CommonSolve.init","text":"CommonSolve.init(prob::AbstractStateSpaceProblem, alg=default_alg(prob); save_everystep=true, kwargs...)\n\nCreate a StateSpaceWorkspace with pre-allocated output arrays and scratch cache. When save_everystep=false, allocates minimal 2-element buffers (endpoints only).\n\n\n\n\n\n","category":"function"},{"location":"basics/workspace/#CommonSolve.solve!","page":"Workspace API","title":"CommonSolve.solve!","text":"CommonSolve.solve!(ws::StateSpaceWorkspace; kwargs...)\n\nSolve the state-space problem. Mutates ws.output arrays in place, then wraps them in a StateSpaceSolution and returns it.\n\n\n\n\n\n","category":"function"},{"location":"tutorials/generic_callbacks/#Generic-Callbacks","page":"Generic Callbacks","title":"Generic Callbacks","text":"The StateSpaceProblem type provides a fully generic interface for discrete-time state-space models. Instead of specifying matrices, you supply callback functions for the state transition and observation equations. This is useful for nonlinear models, time-varying dynamics, or any structure that does not fit the linear or quadratic templates.","category":"section"},{"location":"tutorials/generic_callbacks/#Callback-Signatures","page":"Generic Callbacks","title":"Callback Signatures","text":"The two callbacks follow the \"bang-bang\" convention used throughout SciML: for mutable arrays, mutate the output buffer in place and return it; for immutable arrays (e.g., SVector), ignore the buffer and return a new value.\n\nTransition function: f!!(x_next, x, w, p, t) -> x_next\n\nx_next: pre-allocated output buffer (mutate in place for mutable arrays)\nx: current state\nw: noise shock at this step (or nothing if n_shocks = 0)\np: parameters passed to the problem\nt: integer time index (0-based)\n\nObservation function: g!!(y, x, p, t) -> y\n\ny: pre-allocated output buffer\nx: current state\np: parameters\nt: integer time index (0-based)\n\nPass nothing for the observation function if no observations are needed.","category":"section"},{"location":"tutorials/generic_callbacks/#Example:-Linear-Model-via-Callbacks","page":"Generic Callbacks","title":"Example: Linear Model via Callbacks","text":"We can reproduce the behavior of LinearStateSpaceProblem using generic callbacks. This verifies the interface and demonstrates the pattern.\n\nusing DifferenceEquations, LinearAlgebra, Random\n\nA = [0.95 6.2; 0.0 0.2]\nB = [0.0; 0.01;;]\nC = [0.09 0.67; 1.00 0.00]\n\nlinear_f!! = (x_next, x, w, p, t) -> begin\n    mul!(x_next, p.A, x)\n    mul!(x_next, p.B, w, 1.0, 1.0)\n    return x_next\nend\nlinear_g!! = (y, x, p, t) -> begin\n    mul!(y, p.C, x)\n    return y\nend\np = (; A, B, C)\nu0 = zeros(2)\nT = 10\n\nprob = StateSpaceProblem(linear_f!!, linear_g!!, u0, (0, T), p;\n    n_shocks = 1, n_obs = 2, syms = (:a, :b))\nsol = solve(prob)\n\nThe solution has the same structure as the linear case:\n\nsol.u  # state trajectory, Vector{Vector}\n\nsol.z  # observations, Vector{Vector}\n\nWe can verify this matches the matrix-based formulation:\n\nRandom.seed!(123)\nsol_generic = solve(StateSpaceProblem(linear_f!!, linear_g!!, u0, (0, T), p;\n    n_shocks = 1, n_obs = 2))\n\nRandom.seed!(123)\nsol_linear = solve(LinearStateSpaceProblem(A, B, u0, (0, T); C))\n\nsol_generic.u ≈ sol_linear.u","category":"section"},{"location":"tutorials/generic_callbacks/#Example:-Nonlinear-Growth-Model","page":"Generic Callbacks","title":"Example: Nonlinear Growth Model","text":"StateSpaceProblem handles arbitrary nonlinear dynamics. Here is a discrete-time logistic growth model with process noise, demonstrating that the generic callback interface works for any transition function:\n\n# Nonlinear transition: logistic growth with stochastic shocks\nlogistic_f!! = (x_next, x, w, p, t) -> begin\n    x_next[1] = p.r * x[1] * (1.0 - x[1] / p.K) + p.sigma * w[1]\n    return x_next\nend\n\n# Observation: noisy measurement of population\nlogistic_g!! = (y, x, p, t) -> begin\n    y[1] = x[1]\n    return y\nend\n\np_logistic = (; r = 1.5, K = 100.0, sigma = 2.0)\nu0_logistic = [50.0]\n\nprob_logistic = StateSpaceProblem(logistic_f!!, logistic_g!!, u0_logistic, (0, 50), p_logistic;\n    n_shocks = 1, n_obs = 1, syms = (:population,), obs_syms = (:measured_pop,))\nsol_logistic = solve(prob_logistic)","category":"section"},{"location":"tutorials/generic_callbacks/#Parametric-Models-and-remake","page":"Generic Callbacks","title":"Parametric Models and remake","text":"The p argument holds all model parameters. When exploring different parameter values, use remake to create a new problem without reallocating everything.\n\nnew_u0 = [0.1, 0.2]\nnew_p = (; A = A * 0.99, B, C)\n\nprob2 = remake(prob; u0 = new_u0, p = new_p)\nsol2 = solve(prob2)\nsol2.u[1]  # new initial condition\n\nThe remake function preserves all keyword arguments (noise, observables, syms, etc.) from the original problem.","category":"section"},{"location":"tutorials/generic_callbacks/#Symbolic-Indexing","page":"Generic Callbacks","title":"Symbolic Indexing","text":"StateSpaceProblem supports the same symbolic indexing as the linear problem types. Pass syms for state variable names and obs_syms for observation names.\n\nD = Diagonal([0.1, 0.1])\nnoise = sol.W  # reuse noise from earlier\n\nprob_sym = StateSpaceProblem(linear_f!!, linear_g!!, u0, (0, T), p;\n    n_shocks = 1, n_obs = 2,\n    syms = (:capital, :productivity),\n    obs_syms = (:output, :consumption),\n    observables_noise = D, noise)\nsol_sym = solve(prob_sym)\n\nsol_sym[:capital]  # state time series by name\n\nsol_sym[:output]  # observation time series by name","category":"section"},{"location":"tutorials/linear_likelihood/#Likelihood-and-Kalman-Filter","page":"Likelihood & Kalman Filter","title":"Likelihood & Kalman Filter","text":"DifferenceEquations.jl supports two approaches to computing the log-likelihood of observed data:\n\nMarginal likelihood via the KalmanFilter: the probability of the observed data conditioned on the core model parameters (A B C, etc.) and the initial condition prior, with the latent noise sequence analytically integrated out. This is the standard approach for maximum likelihood estimation (MLE) of structural parameters.\nJoint likelihood via DirectIteration: the probability of the observed data AND a specific noise realization, conditioned on the core parameters and initial conditions. Requires fixing the noise sequence. Useful in Bayesian methods where the noise is sampled as part of inference (e.g., particle MCMC, HMC on latent variables).\n\nBoth approaches are fully differentiable with Enzyme.jl and ForwardDiff.jl.","category":"section"},{"location":"tutorials/linear_likelihood/#Simulating-Observations","page":"Likelihood & Kalman Filter","title":"Simulating Observations","text":"First, let us simulate a model with observation noise to produce synthetic data.\n\nusing DifferenceEquations, LinearAlgebra, Distributions, Random\n\nA = [0.95 6.2; 0.0 0.2]\nB = [0.0; 0.01;;]\nC = [0.09 0.67; 1.00 0.00]\nD = Diagonal([0.1, 0.1])\nu0 = zeros(2)\nT = 80\n\nRandom.seed!(42)\nprob_sim = LinearStateSpaceProblem(A, B, MvNormal(zeros(2), I(2)), (0, T);\n    C, observables_noise = D)\nsol_sim = solve(prob_sim)\nsol_sim.z  # simulated observations with noise (Vector{Vector})","category":"section"},{"location":"tutorials/linear_likelihood/#Marginal-Likelihood-with-the-Kalman-Filter","page":"Likelihood & Kalman Filter","title":"Marginal Likelihood with the Kalman Filter","text":"The Kalman filter computes the marginal log-likelihood by integrating out the latent noise sequence. It requires a Gaussian prior on the initial state (u0_prior_mean, u0_prior_var) and Gaussian observation noise (observables_noise).\n\nnote: Timing convention\nObservations correspond to z_1 z_2 ldots z_T (predictions starting from the second state). When the simulation produces T+1 observation vectors (including z_0), pass sol.z[2:end] as the observables. The length of observables must equal the integer distance of tspan.\n\nobservables = sol_sim.z[2:end]  # Vector{Vector}, length T\n\nu0_prior_mean = zeros(2)\nu0_prior_var = Matrix(1.0 * I(2))\n\nprob_kalman = LinearStateSpaceProblem(A, B, u0, (0, length(observables)); C,\n    observables_noise = D, observables,\n    u0_prior_mean, u0_prior_var)\n\n# KalmanFilter is auto-selected when priors + observables + noise covariance are given\nsol_kalman = solve(prob_kalman)\nsol_kalman.logpdf  # marginal log-likelihood\n\nThe Kalman solution also provides filtered state estimates in sol.u and posterior covariances in sol.P:\n\nsol_kalman.u[end]  # filtered mean at the final time step\n\nsol_kalman.P[end]  # posterior covariance at the final time step","category":"section"},{"location":"tutorials/linear_likelihood/#Joint-Likelihood-with-Fixed-Noise","page":"Likelihood & Kalman Filter","title":"Joint Likelihood with Fixed Noise","text":"When both noise and observables are provided, DirectIteration iterates the state transition forward using the given noise and accumulates the joint log-likelihood of the observations.\n\nnoise = sol_sim.W  # realized noise from simulation (Vector{Vector})\n\nprob_joint = LinearStateSpaceProblem(A, B, u0, (0, length(observables)); C,\n    observables_noise = D, observables, noise)\nsol_joint = solve(prob_joint)\nsol_joint.logpdf  # joint log-likelihood conditioned on noise","category":"section"},{"location":"tutorials/linear_likelihood/#Composing-Structural-Models","page":"Likelihood & Kalman Filter","title":"Composing Structural Models","text":"In practice, the state-space matrices A B C D are often generated from deeper structural (or \"deep\") parameters. The entire pipeline from structural parameters to log-likelihood is differentiable.\n\nfunction generate_model(beta)\n    A = [beta 6.2; 0.0 0.2]\n    B = [0.0; 0.001;;]\n    C = [0.09 0.67; 1.00 0.00]\n    D = Diagonal([0.01, 0.01])\n    return (; A, B, C, D)\nend\n\nfunction kalman_model_likelihood(beta, observables)\n    mod = generate_model(beta)\n    prob = LinearStateSpaceProblem(mod.A, mod.B, zeros(2), (0, length(observables));\n        C = mod.C, observables, observables_noise = mod.D,\n        u0_prior_mean = zeros(2), u0_prior_var = Matrix(1.0 * I(2)))\n    return solve(prob).logpdf\nend\n\nkalman_model_likelihood(0.95, observables)","category":"section"},{"location":"tutorials/linear_likelihood/#Next-Steps","page":"Likelihood & Kalman Filter","title":"Next Steps","text":"To differentiate these likelihoods with Enzyme.jl and use them inside an optimization loop, see the Enzyme AD page. It covers the workspace-based init/solve! pattern required by Enzyme, gradient computation for both DirectIteration and KalmanFilter, and a full maximum-likelihood example with Optimization.jl.","category":"section"},{"location":"advanced/forwarddiff_ad/#ForwardDiff-AD","page":"ForwardDiff AD","title":"ForwardDiff AD","text":"DifferenceEquations.jl works with ForwardDiff.jl out of the box for computing gradients and Jacobians. ForwardDiff requires no shadow arrays, no activity annotations, and no workspace pre-allocation – just wrap your function in ForwardDiff.gradient.\n\ntip: When to use ForwardDiff vs Enzyme\nScenario Recommendation\nSmall models (N ≤ 5 states), few parameters ForwardDiff – same speed as Enzyme reverse, zero setup cost\nLarge models (N ≥ 10 states) Enzyme reverse – scales as O(1) backward passes vs O(N²) forward passes\nMany parameters (e.g., noise perturbation over T periods) Enzyme reverse – ForwardDiff cost scales with parameter count\nQuick prototyping ForwardDiff – simpler API, no Duplicated bookkeeping\nProduction estimation loops Enzyme reverse – lower memory, pre-allocated workspaceFor DirectIteration problems where you differentiate with respect to the noise sequence, the effective parameter dimension is K × T (shocks × periods), not just N². Even for small state dimensions, long horizons make Enzyme reverse the better choice.","category":"section"},{"location":"advanced/forwarddiff_ad/#The-Core-Pattern","page":"ForwardDiff AD","title":"The Core Pattern","text":"ForwardDiff propagates dual numbers through the computation. The key requirement is that all arrays must have a consistent element type (either Float64 or Dual{...}). The pattern is:\n\nWrite a scalar function of a vector. ForwardDiff.gradient takes f: ℝⁿ → ℝ.\nPromote all arrays inside the function. When ForwardDiff.gradient calls your function with a Vector{Dual{...}}, convert all other matrices to the same Dual element type so that caches are allocated correctly.\nUse the public solve() API. Unlike Enzyme, ForwardDiff creates fresh caches each call (with the correct Dual element type), so the simple solve(prob, alg) path works directly.\n\n_promote(::Type{T}, x::AbstractArray{T}) where {T} = x\n_promote(::Type{T}, x::AbstractArray) where {T} = T.(x)","category":"section"},{"location":"advanced/forwarddiff_ad/#Differentiating-Joint-Likelihood","page":"ForwardDiff AD","title":"Differentiating Joint Likelihood","text":"using DifferenceEquations, LinearAlgebra, ForwardDiff, Random\n\nN, K, M = 2, 1, 2\nA = [0.8 0.1; -0.1 0.7]\nB = [0.1; 0.0;;]\nC = [1.0 0.0; 0.0 1.0]\nH = [0.1 0.0; 0.0 0.1]\nu0 = zeros(N)\n\nRandom.seed!(42)\nnoise = [randn(K) for _ in 1:5]\nsim = solve(LinearStateSpaceProblem(A, B, u0, (0, 5); C, noise))\nobs = [sim.z[t + 1] + 0.1 * randn(M) for t in 1:5]\n\n# Type-promotion helper\n_promote(::Type{T}, x::AbstractArray{T}) where {T} = x\n_promote(::Type{T}, x::AbstractArray) where {T} = T.(x)\n\n# Gradient of joint loglik w.r.t. vec(A)\nfunction di_loglik(A_vec, B, C, u0, noise, obs, H)\n    T_el = eltype(A_vec)\n    A = reshape(A_vec, 2, 2)\n    H_d = _promote(T_el, H)\n    R = H_d * H_d'\n    prob = LinearStateSpaceProblem(\n        A, _promote(T_el, B), _promote(T_el, u0), (0, length(obs));\n        C = _promote(T_el, C), observables_noise = R,\n        observables = obs, noise = noise)\n    sol = solve(prob, DirectIteration())\n    return sol.logpdf\nend\n\ngrad_A = ForwardDiff.gradient(\n    a -> di_loglik(a, B, C, u0, noise, obs, H), vec(copy(A)))","category":"section"},{"location":"advanced/forwarddiff_ad/#Differentiating-the-Kalman-Filter","page":"ForwardDiff AD","title":"Differentiating the Kalman Filter","text":"The KalmanFilter marginal log-likelihood works the same way.\n\n# General Kalman loglik that promotes all inputs consistently\nfunction kf_loglik(A, B, C, mu0, Sigma0, R, obs)\n    T_el = promote_type(eltype(A), eltype(B), eltype(C),\n        eltype(mu0), eltype(Sigma0), eltype(R))\n    N_st = size(A, 1)\n    prob = LinearStateSpaceProblem(\n        _promote(T_el, A), _promote(T_el, B),\n        zeros(T_el, N_st), (0, length(obs));\n        C = _promote(T_el, C),\n        u0_prior_mean = _promote(T_el, mu0),\n        u0_prior_var = _promote(T_el, Sigma0),\n        observables_noise = _promote(T_el, R),\n        observables = obs)\n    sol = solve(prob, KalmanFilter())\n    return sol.logpdf\nend\n\nmu0 = zeros(N)\nSigma0 = Matrix(1.0 * I(N))\nR = [0.01 0.0; 0.0 0.01]\n\ngrad_kf = ForwardDiff.gradient(\n    a -> kf_loglik(reshape(a, N, N), B, C, mu0, Sigma0, R, obs), vec(copy(A)))","category":"section"},{"location":"advanced/forwarddiff_ad/#Differentiating-with-Respect-to-Multiple-Parameters","page":"ForwardDiff AD","title":"Differentiating with Respect to Multiple Parameters","text":"Because kf_loglik promotes all inputs via promote_type, you can differentiate with respect to any parameter.\n\n# Gradient w.r.t. observation matrix C\ngrad_C = ForwardDiff.gradient(\n    c_vec -> kf_loglik(A, B, reshape(c_vec, M, N), mu0, Sigma0, R, obs),\n    vec(copy(C)))\n\n# Gradient w.r.t. prior mean\ngrad_mu0 = ForwardDiff.gradient(\n    m -> kf_loglik(A, B, C, m, Sigma0, R, obs), copy(mu0))","category":"section"},{"location":"advanced/forwarddiff_ad/#Integration-with-Optimization.jl","page":"ForwardDiff AD","title":"Integration with Optimization.jl","text":"ForwardDiff integrates with Optimization.jl via AutoForwardDiff(), which is simpler than the Enzyme path (no manual Duplicated bookkeeping).\n\nusing Optimization, OptimizationOptimJL\n\n# Simulate data from a known model\nRandom.seed!(42)\nT_opt = 200\nB_opt = [0.0; 0.001;;]\nC_opt = [0.09 0.67; 1.00 0.00]\nR_opt = [0.01 0.0; 0.0 0.01]\nprob_data = LinearStateSpaceProblem([0.95 6.2; 0.0 0.2], B_opt, zeros(2), (0, T_opt);\n    C = C_opt, observables_noise = R_opt)\nsol_data = solve(prob_data)\nobs_data = sol_data.z[2:end]\n\n# Objective: negative Kalman loglik as function of β = [A[1,1]]\nmu0_opt = zeros(2)\nSigma0_opt = Matrix(1e-2 * I(2))\n\nfunction neg_loglik(beta, p)\n    A = [beta[1] 6.2; 0.0 0.2]\n    return -kf_loglik(A, p.B, p.C, p.mu0, p.Sigma0, p.R, p.obs)\nend\n\nparams = (; B = B_opt, C = C_opt, R = R_opt, obs = obs_data,\n    mu0 = mu0_opt, Sigma0 = Sigma0_opt)\n\noptf = OptimizationFunction(neg_loglik, AutoForwardDiff())\noptprob = OptimizationProblem(optf, [0.90], params)\noptsol = solve(optprob, LBFGS())\noptsol.u  # estimated β (true value: 0.95)","category":"section"},{"location":"advanced/forwarddiff_ad/#StaticArrays","page":"ForwardDiff AD","title":"StaticArrays","text":"ForwardDiff also works with SVector/SMatrix inputs. Construct static arrays from the dual-typed input vector inside the function.\n\nusing StaticArrays\n\nfunction kf_loglik_static(A_vec, B, C, mu0, Sigma0, R, obs,\n        ::Val{N_}, ::Val{M_}, ::Val{K_}) where {N_, M_, K_}\n    T_el = eltype(A_vec)\n    A = SMatrix{N_, N_}(reshape(A_vec, N_, N_))\n    prob = LinearStateSpaceProblem(\n        A, SMatrix{N_, K_}(T_el.(B)),\n        SVector{N_}(zeros(T_el, N_)), (0, length(obs));\n        C = SMatrix{M_, N_}(T_el.(C)),\n        u0_prior_mean = SVector{N_}(T_el.(mu0)),\n        u0_prior_var = SMatrix{N_, N_}(T_el.(Sigma0)),\n        observables_noise = SMatrix{M_, M_}(T_el.(R)),\n        observables = obs)\n    sol = solve(prob, KalmanFilter())\n    return sol.logpdf\nend\n\nobs_s = [SVector{M}(o) for o in obs]\ngrad_static = ForwardDiff.gradient(\n    a -> kf_loglik_static(a, SMatrix{N,K}(B), SMatrix{M,N}(C),\n        SVector{N}(mu0), SMatrix{N,N}(Sigma0), SMatrix{M,M}(R),\n        obs_s, Val(N), Val(M), Val(K)),\n    collect(vec(Matrix(A))))\n\ntip: Use `save_everystep=false` with StaticArrays\nCombining ForwardDiff + StaticArrays with save_everystep=false gives the best performance for small models. Without it, the allocation of T dual-number SVector buffers dominates. With save_everystep=false, only 2 scratch slots are used, yielding up to 7x speedup for the Kalman filter and 3.4x for ConditionalLikelihood at N=5.sol = solve(prob, KalmanFilter(); save_everystep=false)","category":"section"},{"location":"advanced/forwarddiff_ad/#Quadratic-and-Generic-Models","page":"ForwardDiff AD","title":"Quadratic and Generic Models","text":"ForwardDiff works with all problem types: QuadraticStateSpaceProblem, PrunedQuadraticStateSpaceProblem, and StateSpaceProblem. The same pattern applies — promote all arrays to the Dual element type inside the gradient function and call solve(prob, DirectIteration()).","category":"section"},{"location":"advanced/forwarddiff_ad/#Important-Notes","page":"ForwardDiff AD","title":"Important Notes","text":"Type promotion is required. All arrays flowing into the problem must have the same element type. Use promote_type across all inputs (as in kf_loglik above) or the _promote helper to convert Float64 arrays to the Dual type.\nFresh allocation each call. ForwardDiff creates new caches with Dual element types via solve(). This is unavoidable (unlike Enzyme, which reuses Float64 caches with separate shadow arrays). Use save_everystep=false to minimize these allocations from O(T) to O(1) when you only need logpdf.\nChunk size. ForwardDiff.gradient defaults to a chunk size of ~10, processing 10 partial derivatives per forward pass. For parameter count > 10, it runs multiple passes. This makes ForwardDiff cost scale linearly with the number of parameters being differentiated.\nObservations stay Float64. The observables (data) are not differentiated and can remain Vector{Vector{Float64}}. The solver's internal buffers are allocated with the Dual element type, so when Float64 observations are copied in, the dual partials are zero — which is correct since observations are data, not parameters being differentiated.\nDirectIteration noise sensitivity. When differentiating DirectIteration w.r.t. the noise sequence, the parameter dimension is K × T (shocks × periods). Even for small state-space models, long time series make ForwardDiff expensive and Enzyme reverse the better choice.","category":"section"},{"location":"advanced/static_arrays/#StaticArrays","page":"StaticArrays","title":"StaticArrays","text":"For small state-space models (typically 2–5 states), using StaticArrays.jl can significantly improve performance by eliminating heap allocations and enabling compiler optimizations such as loop unrolling.","category":"section"},{"location":"advanced/static_arrays/#Example","page":"StaticArrays","title":"Example","text":"using DifferenceEquations, StaticArrays, LinearAlgebra\nA = @SMatrix [0.95 6.2; 0.0 0.2]\nB = @SMatrix [0.0; 0.01;;]\nC = @SMatrix [0.09 0.67; 1.00 0.00]\nu0 = @SVector zeros(2)\nprob = LinearStateSpaceProblem(A, B, u0, (0, 10); C)\nsol = solve(prob)\nsol.u[end]","category":"section"},{"location":"advanced/static_arrays/#When-to-Use","page":"StaticArrays","title":"When to Use","text":"StaticArrays are most beneficial when:\n\nState dimensions are small: The performance advantage is greatest for matrices up to roughly 10x10. Beyond that, the compile-time overhead and code size can outweigh the benefits.\nSizes are known at compile time: StaticArrays encode their dimensions as type parameters, so the sizes must be fixed constants rather than runtime values.\nYou need stack allocation: StaticArrays are stored on the stack rather than the heap, eliminating GC pressure entirely for small models.\n\nFor larger models or models where dimensions vary at runtime, use standard Array types instead.","category":"section"},{"location":"advanced/static_arrays/#Bang-Bang-Operators","page":"StaticArrays","title":"Bang-Bang Operators","text":"The package internally uses \"bang-bang\" operators (e.g., mul!!, copyto!!, assign!!) that handle both mutable and immutable arrays transparently. When you pass SMatrix and SVector types, these operators return new immutable values rather than mutating in place. When you pass standard Matrix and Vector types, they mutate in place and return the result. This means you do not need to change any solver code to switch between static and dynamic arrays – simply change the array types in your problem definition.\n\nSee Internals for the full list of bang-bang operators and their behavior.","category":"section"},{"location":"advanced/static_arrays/#Supported-Problem-Types","page":"StaticArrays","title":"Supported Problem Types","text":"StaticArrays work with all problem types:\n\nLinearStateSpaceProblem with DirectIteration — simulation and log-likelihood\nLinearStateSpaceProblem with KalmanFilter — filtering, smoothing, and log-likelihood\nQuadraticStateSpaceProblem and PrunedQuadraticStateSpaceProblem — second-order perturbation models\nStateSpaceProblem — generic callbacks using bang-bang operators","category":"section"},{"location":"advanced/static_arrays/#Kalman-Filter-Example","page":"StaticArrays","title":"Kalman Filter Example","text":"using DifferenceEquations, StaticArrays, LinearAlgebra, Random\nRandom.seed!(42)\nA = SMatrix{2,2}(0.8*I(2))\nB = SMatrix{2,1}([0.1; 0.05])\nC = SMatrix{2,2}(1.0*I(2))\nR = SMatrix{2,2}(0.01*I(2))\nmu0 = @SVector zeros(2)\nSig0 = SMatrix{2,2}(1.0*I(2))\ny = [SVector{2}(randn(2)) for _ in 1:10]\nprob = LinearStateSpaceProblem(A, B, mu0, (0, 10); C,\n    u0_prior_mean=mu0, u0_prior_var=Sig0,\n    observables_noise=R, observables=y)\nsol = solve(prob, KalmanFilter())\nsol.logpdf","category":"section"},{"location":"advanced/static_arrays/#AD-Performance-Note","page":"StaticArrays","title":"AD Performance Note","text":"Enzyme reverse-mode AD benefits significantly from StaticArrays at small dimensions (N ≤ 5), with 5–7x speedups over mutable arrays for both the primal and AD passes.\n\nForwardDiff with StaticArrays does not improve AD performance for this package. The overhead of constructing SMatrix{N,N,Dual{...}} temporaries outweighs the benefit. See ForwardDiff AD for details.","category":"section"},{"location":"basics/faq/#FAQ","page":"FAQ","title":"FAQ","text":"","category":"section"},{"location":"basics/faq/#When-should-I-use-the-Kalman-filter-vs.-joint-likelihood?","page":"FAQ","title":"When should I use the Kalman filter vs. joint likelihood?","text":"Kalman filter: Use for linear Gaussian models when you want the marginal likelihood, integrating out the latent noise sequence. This is the standard approach for maximum likelihood estimation (MLE) of parameters.\nJoint likelihood: Use when conditioning on a specific noise realization. This is useful for Bayesian methods where the noise sequence is sampled as part of inference (e.g., particle MCMC, HMC on latent variables).","category":"section"},{"location":"basics/faq/#Why-does-Enzyme-require-all-arguments-to-be-Duplicated?","page":"FAQ","title":"Why does Enzyme require all arguments to be Duplicated?","text":"Enzyme tracks activity at the struct level. When constructing a LinearStateSpaceProblem, all matrix arguments (e.g., A, B, C) flow into a single struct. If any argument is active (i.e., being differentiated), Enzyme needs shadow copies for all arguments in the struct. Passing some arguments as Const while others are Duplicated triggers an EnzymeRuntimeActivityError. The solution is to mark all arguments as Duplicated.","category":"section"},{"location":"basics/faq/#What-is-the-observables-timing-convention?","page":"FAQ","title":"What is the observables timing convention?","text":"The tspan (0, T) produces T+1 states: u_0 u_1 ldots u_T. Observations z_n correspond to state u_n. The observables keyword expects T vectors corresponding to z_1 z_2 ldots z_T (skipping z_0). So when passing simulated data, use sol.z[2:end].","category":"section"},{"location":"basics/faq/#What-does-observables_noise-represent?","page":"FAQ","title":"What does observables_noise represent?","text":"The observables_noise keyword specifies the observation noise covariance matrix (entries are variances and covariances, not standard deviations). It must be an AbstractMatrix — use Diagonal([σ₁², σ₂², …]) for diagonal noise or a full Matrix/Symmetric(H * H') for correlated noise.\n\nIts behavior depends on context:\n\nDuring simulation (when observables is not provided): used to generate synthetic measurement noise added to the clean observations sol.z.\nDuring likelihood computation (when observables is provided): used as the observation noise covariance in the log-likelihood calculation.","category":"section"},{"location":"basics/faq/#How-do-I-differentiate-with-respect-to-only-some-parameters?","page":"FAQ","title":"How do I differentiate with respect to only some parameters?","text":"Enzyme: All arguments to the likelihood function must be marked Duplicated (see Enzyme AD). However, you only need to read the shadow of the parameter you care about — the other shadows are computed but can be discarded. There is no performance cost to ignoring unused shadows.\nForwardDiff: Only the parameter passed through ForwardDiff.gradient's vector argument is differentiated. Other parameters are captured as constants in the closure, so no derivatives are computed for them.","category":"section"},{"location":"basics/faq/#How-do-I-choose-between-QuadraticStateSpaceProblem-and-PrunedQuadraticStateSpaceProblem?","page":"FAQ","title":"How do I choose between QuadraticStateSpaceProblem and PrunedQuadraticStateSpaceProblem?","text":"PrunedQuadraticStateSpaceProblem: Use for second-order perturbation solutions of DSGE models. The pruning prevents explosive dynamics by applying the quadratic term to a separate linear-part state rather than the full nonlinear state.\nQuadraticStateSpaceProblem: Use if you specifically need the unpruned quadratic form (e.g., for comparison or when the system is known to be stable).","category":"section"},{"location":"basics/solutions/#Solutions","page":"Solutions","title":"Solutions","text":"","category":"section"},{"location":"basics/solutions/#Fields","page":"Solutions","title":"Fields","text":"Field Type Description\nu Vector{Vector{T}} State trajectory\nt Range Time values\nz Vector{Vector{T}} or nothing Observations\nW Vector{Vector{T}} or nothing Noise sequence (DirectIteration only)\nP Vector{Matrix{T}} or nothing Posterior covariances (KalmanFilter only)\nlogpdf Real Log-likelihood (0.0 if no observables; may be a Dual number under ForwardDiff)\nretcode ReturnCode.T ReturnCode.Success (errors are thrown as exceptions, not encoded in the return code)\nprob Problem Original problem\nalg Algorithm Algorithm used","category":"section"},{"location":"basics/solutions/#Symbolic-Indexing","page":"Solutions","title":"Symbolic Indexing","text":"If syms or obs_syms were provided when constructing the problem, the solution supports symbolic indexing:\n\nprob = LinearStateSpaceProblem(A, B, u0, (0, 10); C, syms=(:x, :y), obs_syms=(:obs1, :obs2))\nsol = solve(prob)\n\n# Access state variables by name\nsol[:x]    # vector of :x values across all time steps\nsol[:obs1] # vector of :obs1 observations across all time steps","category":"section"},{"location":"basics/solutions/#Standard-Indexing","page":"Solutions","title":"Standard Indexing","text":"Solutions support standard Julia indexing to access states at specific time steps:\n\nsol = solve(prob)\n\nsol[1]     # state at t=0 (initial condition)\nsol[end]   # state at the final time step\nsol.u[3]   # state at the third time index\nsol.z[2]   # observation at the second time index (if C was provided)","category":"section"},{"location":"basics/solutions/#DataFrame-Conversion","page":"Solutions","title":"DataFrame Conversion","text":"The state trajectory can be converted to a DataFrame. Column names come from syms if provided. Note that only the state variables (not observations) appear in the DataFrame.\n\nusing DifferenceEquations, LinearAlgebra, DataFrames\nA = [0.95 6.2; 0.0 0.2]\nB = [0.0; 0.01;;]\nC = [0.09 0.67; 1.00 0.00]\nprob = LinearStateSpaceProblem(A, B, zeros(2), (0, 5); C,\n    syms = (:capital, :productivity), obs_syms = (:output, :investment))\nsol = solve(prob)\nDataFrame(sol)","category":"section"},{"location":"basics/solutions/#DifferenceEquations.StateSpaceSolution","page":"Solutions","title":"DifferenceEquations.StateSpaceSolution","text":"StateSpaceSolution\n\nSolution type returned by solve for all state-space problems.\n\nFields\n\nu: State trajectory as Vector{Vector{T}}.\nt: Time values.\nz: Observation trajectory as Vector{Vector{T}}, or nothing.\nW: Noise sequence as Vector{Vector{T}}, or nothing (e.g., for KalmanFilter).\nP: Posterior covariances as Vector{Matrix{T}} (KalmanFilter only), or nothing.\nlogpdf: Log-likelihood value. Zero when no observables are provided.\nretcode: ReturnCode.Success. Errors are thrown as exceptions, not encoded in the return code.\nprob: The original problem.\nalg: The algorithm used.\n\nSymbolic Indexing\n\nAccess time series by symbol name:\n\nsol[:x]      # state variable time series (requires `syms`)\nsol[:output] # observation time series (requires `obs_syms`)\n\nStandard Indexing\n\nsol[i]       # state at time step i (same as sol.u[i])\nsol[end]     # final state\n\n\n\n\n\n","category":"type"},{"location":"basics/solvers/#Solvers","page":"Solvers","title":"Solvers","text":"Solving a state-space problem is as simple as calling solve(prob), which automatically selects an appropriate algorithm. You can also pass an algorithm explicitly via solve(prob, alg).","category":"section"},{"location":"basics/solvers/#Default-Algorithm-Selection","page":"Solvers","title":"Default Algorithm Selection","text":"When no algorithm is specified, solve(prob) selects the algorithm based on the problem type and its fields:\n\nDirectIteration is the default for all problem types. It simulates the state-space model forward in time, generating states and observations directly. If observables are provided, it computes the joint log-likelihood of the observed data given the noise sequence.\nKalmanFilter is auto-selected for LinearStateSpaceProblem when all of the following conditions hold:\nu0_prior_var is an AbstractMatrix (prior covariance is specified)\nnoise is nothing (noise is not fixed)\nobservables is an AbstractVector (observed data is provided)\nobservables_noise is an AbstractMatrix (observation noise covariance is specified)\nA, B, and C are all AbstractMatrix (not nothing)\nThe Kalman filter computes the filtered state estimates and the marginal log-likelihood of the observations, integrating over the unknown noise sequence.\nConditionalLikelihood is never auto-selected. You must pass it explicitly via solve(prob, ConditionalLikelihood()). Use it for fully-observed state-space models (AR, VAR, nonlinear) where the state is directly observed and you want the prediction error decomposition log-likelihood. Works with all problem types.\n\nwarning: Warning\nIf any of the KalmanFilter conditions are not met, DirectIteration is silently selected instead. For example, forgetting to pass C or u0_prior_var will produce a DirectIteration solve with logpdf = 0.0 rather than the expected Kalman filter result.","category":"section"},{"location":"basics/solvers/#save_everystep-Keyword","page":"Solvers","title":"save_everystep Keyword","text":"All algorithms support save_everystep=false, which stores only the initial and final states instead of the full trajectory:\n\nsol = solve(prob; save_everystep=false)              # 2-element sol.u\nsol = solve(prob, ConditionalLikelihood(); save_everystep=false)\nsol = solve(prob, KalmanFilter(); save_everystep=false)\n\nWhen save_everystep=false:\n\nsol.u contains [u_initial, u_final] (2 entries instead of T+1)\nsol.z contains [z_initial, z_final] (if observations are present)\nsol.P contains [P_initial, P_final] (KalmanFilter only)\nsol.logpdf is identical — computed on the fly, not from stored trajectory\n\nThis is useful when you only need the final state or the log-likelihood (e.g., in optimization loops). It dramatically reduces memory allocation, which benefits ForwardDiff gradient computation:\n\nScenario Typical speedup Allocation reduction\nForwardDiff + StaticArrays (KF, N=5) 7x 4,288 → 175\nForwardDiff + StaticArrays (CL, N=5) 3.4x 805 → 190\nForwardDiff + mutable (KF, N=30) 1.5x 342k → 8k\n\nThe workspace API also supports it:\n\nws = init(prob, alg; save_everystep=false)\nsol = solve!(ws)   # reads save_everystep from workspace","category":"section"},{"location":"basics/solvers/#DifferenceEquations.DirectIteration","page":"Solvers","title":"DifferenceEquations.DirectIteration","text":"DirectIteration()\n\nForward iteration algorithm for state-space problems. Iterates the state transition equation forward in time, computing the state trajectory u, observations z, noise history W, and (if observables are provided) the joint log-likelihood logpdf.\n\nThis is the default algorithm for all problem types.\n\nSee also: KalmanFilter.\n\n\n\n\n\n","category":"type"},{"location":"basics/solvers/#DifferenceEquations.KalmanFilter","page":"Solvers","title":"DifferenceEquations.KalmanFilter","text":"KalmanFilter()\n\nKalman filter algorithm for LinearStateSpaceProblem. Computes filtered state estimates, posterior covariances, and the marginal log-likelihood.\n\nAutomatically selected when the problem provides:\n\nu0_prior_mean and u0_prior_var (Gaussian prior),\nobservables (observed data),\nobservables_noise (observation noise covariance),\nnoise = nothing (latent noise is not fixed).\n\nThe solution contains filtered means in sol.u, posterior covariances in sol.P, predicted observations in sol.z, and the marginal log-likelihood in sol.logpdf.\n\nSee also: DirectIteration.\n\n\n\n\n\n","category":"type"},{"location":"basics/solvers/#DifferenceEquations.ConditionalLikelihood","page":"Solvers","title":"DifferenceEquations.ConditionalLikelihood","text":"ConditionalLikelihood()\n\nConditional likelihood (prediction error decomposition) algorithm for fully-observed state-space models. At each step, predicts the next observation from the observed current state using the transition equation, and accumulates the Gaussian log-likelihood of the innovation.\n\nWorks with all problem types (LinearStateSpaceProblem, StateSpaceProblem, QuadraticStateSpaceProblem, PrunedQuadraticStateSpaceProblem). The only requirement is additive Gaussian observation noise.\n\nRequires:\n\nobservables (observed data y₁, …, y_T),\nobservables_noise (innovation covariance R).\n\nThe solution contains predicted observations in sol.z (when an observation equation is present), the conditional log-likelihood in sol.logpdf, and the state trajectory (clamped to observables) in sol.u.\n\nSee also: DirectIteration, KalmanFilter.\n\n\n\n\n\n","category":"type"},{"location":"tutorials/conditional_likelihood/#Conditional-Likelihood","page":"Conditional Likelihood","title":"Conditional Likelihood","text":"The ConditionalLikelihood algorithm computes the prediction error decomposition log-likelihood for fully-observed state-space models. At each time step, it predicts the next observation from the observed current state (not the model-predicted state), and accumulates the Gaussian log-likelihood of the innovation (prediction error).\n\nThis is the standard approach for maximum likelihood estimation of AR(1), VAR(1), nonlinear DSGE, and other models where the state is directly observed.","category":"section"},{"location":"tutorials/conditional_likelihood/#When-to-Use-Each-Algorithm","page":"Conditional Likelihood","title":"When to Use Each Algorithm","text":"Algorithm Use Case\nDirectIteration Simulation, or joint likelihood given a fixed noise sequence\nKalmanFilter Marginal likelihood for linear models with latent (unobserved) noise\nConditionalLikelihood MLE for fully-observed models (AR, VAR, nonlinear)","category":"section"},{"location":"tutorials/conditional_likelihood/#Mathematical-Formulation","page":"Conditional Likelihood","title":"Mathematical Formulation","text":"Given a state-space model with transition x_t+1 = f(x_t w_t) and observation z_t = g(x_t), the conditional log-likelihood is:\n\nlog L = sum_t=1^T left -frac12 left( M log(2pi) + logR + nu_t^top R^-1 nu_t right) right\n\nwhere nu_t = y_t - g(f(y_t-1 w_t)) is the innovation (prediction error), R is the observation noise covariance, and M is the observation dimension.\n\nThe key difference from DirectIteration is that at each step the state is clamped to the observation: the prediction uses f(y_t-1 ldots) rather than f(f(ldots u_0) ldots).","category":"section"},{"location":"tutorials/conditional_likelihood/#AR(1)-Example","page":"Conditional Likelihood","title":"AR(1) Example","text":"using DifferenceEquations, LinearAlgebra, Random\n\nrho_true = 0.8\nsigma_e = 0.5\nT = 200\n\n# Simulate AR(1) data using the package\nRandom.seed!(42)\nprob_sim = LinearStateSpaceProblem(\n    fill(rho_true, 1, 1), fill(sigma_e, 1, 1), [0.0], (0, T))\nsol_sim = solve(prob_sim)\ny = sol_sim.u[2:end]  # observed states y_1, ..., y_T\n\n# Compute conditional log-likelihood\nprob = LinearStateSpaceProblem(\n    fill(rho_true, 1, 1), nothing, [0.0], (0, T);\n    observables = y,\n    observables_noise = Diagonal([sigma_e^2]),\n)\nsol = solve(prob, ConditionalLikelihood())\nsol.logpdf","category":"section"},{"location":"tutorials/conditional_likelihood/#VAR(1)-Example","page":"Conditional Likelihood","title":"VAR(1) Example","text":"The same approach works for multivariate models:\n\nA = [0.8 0.1; -0.1 0.7]\nB = [0.5 0.0; 0.0 0.5]\nT_var = 100\n\n# Simulate VAR(1) data\nRandom.seed!(123)\nprob_sim_var = LinearStateSpaceProblem(A, B, zeros(2), (0, T_var))\nsol_sim_var = solve(prob_sim_var)\ny_var = sol_sim_var.u[2:end]\n\nprob_var = LinearStateSpaceProblem(\n    A, nothing, zeros(2), (0, T_var);\n    observables = y_var,\n    observables_noise = Diagonal([0.25, 0.25]),\n)\nsol_var = solve(prob_var, ConditionalLikelihood())\nsol_var.logpdf","category":"section"},{"location":"tutorials/conditional_likelihood/#Nonlinear-Example-with-StateSpaceProblem","page":"Conditional Likelihood","title":"Nonlinear Example with StateSpaceProblem","text":"ConditionalLikelihood works with all problem types, including user-defined nonlinear callbacks via StateSpaceProblem.\n\nHere we estimate a nonlinear AR(1): x_t+1 = rho x_t + alpha x_t^2 + e_t. We first simulate data using a generic StateSpaceProblem, then compute the conditional likelihood.\n\nrho_nl = 0.8\nalpha_nl = 0.05\nsigma_nl = 0.3\nT_nl = 100\n\n# Nonlinear transition (supports both mutable and immutable arrays)\nfunction nl_transition!!(x_next, x, w, p, t)\n    (; rho, alpha) = p\n    val = rho * x[1] + alpha * x[1]^2\n    if ismutable(x_next)\n        x_next[1] = val + w[1]\n        return x_next\n    else\n        return typeof(x)(val + w[1])\n    end\nend\n\np_nl = (; rho = rho_nl, alpha = alpha_nl)\n\n# Simulate nonlinear data\nRandom.seed!(99)\nprob_sim_nl = StateSpaceProblem(\n    nl_transition!!, nothing, [0.0], (0, T_nl), p_nl;\n    n_shocks = 1, n_obs = 0)\nsol_sim_nl = solve(prob_sim_nl)\ny_nl = sol_sim_nl.u[2:end]\n\n# Conditional likelihood (no noise in prediction, noise only in obs)\nfunction nl_transition_noiseless!!(x_next, x, w, p, t)\n    (; rho, alpha) = p\n    val = rho * x[1] + alpha * x[1]^2\n    if ismutable(x_next)\n        x_next[1] = val\n        return x_next\n    else\n        return typeof(x)(val)\n    end\nend\n\nprob_nl = StateSpaceProblem(\n    nl_transition_noiseless!!, nothing, [0.0], (0, T_nl), p_nl;\n    n_shocks = 0, n_obs = 0,\n    observables = y_nl,\n    observables_noise = Diagonal([sigma_nl^2]),\n)\nsol_nl = solve(prob_nl, ConditionalLikelihood())\nsol_nl.logpdf","category":"section"},{"location":"tutorials/conditional_likelihood/#Maximum-Likelihood-Estimation","page":"Conditional Likelihood","title":"Maximum Likelihood Estimation","text":"ConditionalLikelihood is fully differentiable with ForwardDiff.jl, making it straightforward to use with gradient-based optimization for MLE.\n\nUse save_everystep=false when you only need the log-likelihood (not the full trajectory). This reduces allocations and speeds up ForwardDiff gradient computation — up to 7x faster with StaticArrays.\n\nusing ForwardDiff\n\n# Negative log-likelihood as a function of rho\nfunction neg_loglik(rho_vec)\n    T_el = eltype(rho_vec)\n    A_opt = fill(rho_vec[1], 1, 1)\n    prob_opt = LinearStateSpaceProblem(\n        A_opt, nothing, [zero(T_el)], (0, length(y));\n        observables = y,\n        observables_noise = Diagonal([T_el(sigma_e^2)]),\n    )\n    return -solve(prob_opt, ConditionalLikelihood(); save_everystep = false).logpdf\nend\n\n# Gradient at the true value\ngrad = ForwardDiff.gradient(neg_loglik, [rho_true])\n\nThe gradient is near zero at the true parameter value, confirming the MLE is correctly identified. For full optimization, use Optimization.jl with AutoForwardDiff() and an optimizer like LBFGS().","category":"section"},{"location":"tutorials/conditional_likelihood/#Using-the-Workspace","page":"Conditional Likelihood","title":"Using the Workspace","text":"For repeated solves (e.g., inside an optimizer), use the init/solve! pattern to avoid repeated memory allocation:\n\nws = init(prob, ConditionalLikelihood())\nsol_ws = solve!(ws)\nsol_ws.logpdf\n\nWith save_everystep=false, the workspace allocates only 2-element buffers:\n\nws_ep = init(prob, ConditionalLikelihood(); save_everystep = false)\nsol_ep = solve!(ws_ep)\nlength(sol_ep.u)  # 2: [u_initial, u_final]","category":"section"},{"location":"tutorials/linear_simulation/#Linear-Simulation","page":"Linear Simulation","title":"Linear Simulation","text":"This tutorial walks through simulating a linear time-invariant state-space model of the form\n\nu_n+1 = A  u_n + B  w_n+1\n\nwith observation equation\n\nz_n = C  u_n + v_n\n\nwhere w_n+1 sim N(0 I) and optionally v_n sim N(0 D).","category":"section"},{"location":"tutorials/linear_simulation/#Simulating-a-Linear-State-Space-Model","page":"Linear Simulation","title":"Simulating a Linear State Space Model","text":"We begin by defining system matrices and creating a LinearStateSpaceProblem. Passing C enables the observation equation, observables_noise adds Gaussian measurement noise to the simulated observations, and syms attaches symbolic names to the state variables.\n\nusing DifferenceEquations, LinearAlgebra, Distributions, Random, Plots, DiffEqBase\n\nA = [0.95 6.2; 0.0 0.2]\nB = [0.0; 0.01;;]\nC = [0.09 0.67; 1.00 0.00]\nD = Diagonal([0.1, 0.1])\nu0 = zeros(2)\nT = 10\n\nprob = LinearStateSpaceProblem(A, B, u0, (0, T); C, observables_noise = D, syms = (:a, :b))\nsol = solve(prob)","category":"section"},{"location":"tutorials/linear_simulation/#Plotting","page":"Linear Simulation","title":"Plotting","text":"The solution object integrates with Plots.jl recipes. When syms are provided, the legend labels correspond to those names.\n\nplot(sol)","category":"section"},{"location":"tutorials/linear_simulation/#Accessing-the-Solution","page":"Linear Simulation","title":"Accessing the Solution","text":"The state trajectory is stored in sol.u as a Vector{Vector}. Standard indexing works on the solution object directly.\n\nsol.u  # full state trajectory, Vector{Vector}\n\nAccess a specific time step:\n\nsol[2]  # state at t=1 (second entry; sol[1] is the initial condition u₀)\n\nOr a specific element of the last state:\n\nsol[end][1]  # first element of the final state\n\nObservations and noise are also available:\n\nsol.z  # observed trajectory, Vector{Vector}\n\nsol.W  # realized noise sequence, Vector{Vector}","category":"section"},{"location":"tutorials/linear_simulation/#Symbolic-Indexing","page":"Linear Simulation","title":"Symbolic Indexing","text":"When syms are provided, you can extract the full time series for a state variable by name:\n\nsol[:a]  # time series for state variable :a\n\nIf obs_syms are also provided, observation variables can be accessed similarly:\n\nprob_obs = LinearStateSpaceProblem(A, B, u0, (0, T); C, observables_noise = D,\n    syms = (:a, :b), obs_syms = (:output, :consumption))\nsol_obs = solve(prob_obs)\nsol_obs[:output]  # time series for observation :output","category":"section"},{"location":"tutorials/linear_simulation/#Fixed-Noise","page":"Linear Simulation","title":"Fixed Noise","text":"We can extract the noise from a previous simulation and use it to reproduce a trajectory (possibly with different initial conditions). This is essential for joint likelihood computations.\n\nnoise = sol.W  # extract realized noise (Vector{Vector})\nu0_new = [0.1, 0.0]\nprob_fixed = LinearStateSpaceProblem(A, B, u0_new, (0, T); C, observables_noise = D,\n    syms = (:a, :b), noise)\nsol_fixed = solve(prob_fixed)\nplot(sol_fixed)","category":"section"},{"location":"tutorials/linear_simulation/#Impulse-Response-Functions","page":"Linear Simulation","title":"Impulse Response Functions","text":"An impulse response function (IRF) applies a one-time unit shock at the first period and traces the system's response. We construct this by passing a fixed noise sequence where only the first entry is nonzero.\n\nfunction irf(A, B, C, T = 20)\n    noise = [[i == 1 ? 1.0 : 0.0] for i in 1:T]\n    problem = LinearStateSpaceProblem(A, B, zeros(2), (0, T); C, noise, syms = (:a, :b))\n    return solve(problem)\nend\nplot(irf(A, B, C))","category":"section"},{"location":"tutorials/linear_simulation/#Deterministic-Dynamics-(B-nothing)","page":"Linear Simulation","title":"Deterministic Dynamics (B = nothing)","text":"When the model has no process noise, pass B = nothing. The solver will skip noise generation entirely. No sol.W is produced.\n\nprob_det = LinearStateSpaceProblem(A, nothing, [1.0, 0.5], (0, T); C, syms = (:a, :b))\nsol_det = solve(prob_det)\nsol_det.W === nothing  # no noise generated\n\nplot(sol_det)","category":"section"},{"location":"tutorials/linear_simulation/#No-Observation-Equation-(C-nothing)","page":"Linear Simulation","title":"No Observation Equation (C = nothing)","text":"When you only need the state trajectory and don't require observations, omit C (or pass C = nothing). No sol.z is produced.\n\nprob_no_obs = LinearStateSpaceProblem(A, B, u0, (0, T); syms = (:a, :b))\nsol_no_obs = solve(prob_no_obs)\nsol_no_obs.z === nothing  # no observations\n\nplot(sol_no_obs)","category":"section"},{"location":"tutorials/linear_simulation/#Random-Initial-Conditions","page":"Linear Simulation","title":"Random Initial Conditions","text":"Passing a Distribution for u0 draws a random initial state at each solve.\n\nu0_dist = MvNormal([1.0 0.1; 0.1 1.0])  # zero-mean Gaussian\nprob_rand = LinearStateSpaceProblem(A, nothing, u0_dist, (0, T); C)\nsol_rand = solve(prob_rand)\nplot(sol_rand)","category":"section"},{"location":"tutorials/linear_simulation/#Ensemble-Simulations","page":"Linear Simulation","title":"Ensemble Simulations","text":"The SciML EnsembleProblem interface runs many independent simulations in parallel. Each trajectory draws a fresh initial condition (when u0 is a distribution) and/or fresh noise. EnsembleSummary computes quantile bands across the ensemble.\n\ntrajectories = 50\nprob_ens = LinearStateSpaceProblem(A, B, u0_dist, (0, T); C)\nensemble_sol = solve(EnsembleProblem(prob_ens), DirectIteration(), EnsembleThreads();\n    trajectories)\nsumm = EnsembleSummary(ensemble_sol)\nplot(summ)","category":"section"},{"location":"advanced/internals/#Internals","page":"Internals","title":"Internals","text":"This page documents the internal architecture of DifferenceEquations.jl. It is intended for developers who want to understand the package internals or extend the package with new problem types or algorithms.","category":"section"},{"location":"advanced/internals/#Architecture","page":"Internals","title":"Architecture","text":"The solving pipeline follows these stages:\n\nProblem construction: The user creates a problem (e.g., LinearStateSpaceProblem) that encodes the model dynamics, parameters, and data.\nAlgorithm dispatch: solve(prob) or solve(prob, alg) selects the algorithm. If no algorithm is provided, the default is chosen based on the problem type and its fields.\nWorkspace allocation: init(prob, alg) allocates the solution output via alloc_sol and scratch workspace via alloc_cache, then wraps them in a StateSpaceWorkspace.\nSolve: solve!(ws) runs the algorithm, which fully overwrites all solution and cache arrays during the time loop.\nSolution: A StateSpaceSolution is returned containing the state trajectory, observations, noise, log-likelihood, and other results.","category":"section"},{"location":"advanced/internals/#Bang-Bang-Operators","page":"Internals","title":"Bang-Bang Operators","text":"DifferenceEquations.jl uses a \"bang-bang\" (!!) convention for internal operators. These functions behave differently depending on whether their arguments are mutable or immutable:\n\nMutable arrays (Vector, Matrix): The operator mutates the destination in place and returns it.\nImmutable arrays (SVector, SMatrix): The operator creates and returns a new value, since mutation is not possible.\n\nThis dual behavior allows the same algorithm code to work with both standard arrays and StaticArrays without any branching or specialization at the call site.\n\nThe main bang-bang operators are:\n\nOperator Description\nmul!!(C, A, B) Matrix multiply A * B, storing in C\ncopyto!!(dest, src) Copy contents of src into dest\nassign!!(dest, i, val) Assign val to position i in dest\ncholesky!!(F, A) Compute the Cholesky factorization of A\nldiv!!(Y, F, B) Solve F \\ B, storing in Y\ntranspose!!(dest, src) Transpose src into dest","category":"section"},{"location":"advanced/internals/#Cache-System","page":"Internals","title":"Cache System","text":"Each combination of problem type and algorithm defines two allocation functions:\n\nalloc_sol(prob, alg, T): Allocates the output structure that will hold the solution (state trajectory, observations, noise, covariances, etc.). Returns a named tuple or struct of pre-allocated arrays.\nalloc_cache(prob, alg, T): Allocates scratch workspace needed during the solve (temporary vectors, matrices for intermediate computations, etc.). Returns a named tuple or struct of pre-allocated buffers.\n\nThe solver loop fully overwrites all solution and cache arrays on each call, so no explicit zeroing step is needed between calls. For Enzyme AD, shadow copies should be zero-initialized via Enzyme.make_zero(deepcopy(...)) at creation time (see Enzyme AD).","category":"section"},{"location":"advanced/internals/#Adding-a-New-Problem-Type","page":"Internals","title":"Adding a New Problem Type","text":"To add a new problem type to DifferenceEquations.jl, you need to implement the following methods:\n\nMethod Signature Description\n_noise_matrix(prob) prob → Matrix Return the noise input matrix (e.g., B for linear models)\n_init_model_state!!(prob, cache) prob, cache → cache Initialize any model-specific cache state before the time loop\n_transition!!(x_next, x, w, prob, cache, t) x_next, x, w, prob, cache, t → x_next Compute the next state given current state x and noise w at time t\n_observation!!(y, x, prob, cache, t) y, x, prob, cache, t → y Compute the observation given state x at time t\nalloc_sol(prob, alg, T) prob, alg, Int → NamedTuple Allocate the solution output arrays for T time steps\nalloc_cache(prob, alg, T) prob, alg, Int → NamedTuple Allocate scratch workspace for T time steps\n\nAll transition and observation methods should follow the bang-bang convention: mutate the first argument if it is mutable, otherwise return a new value. This ensures compatibility with both standard arrays and StaticArrays.","category":"section"},{"location":"tutorials/quadratic/#Quadratic-Models","page":"Quadratic Models","title":"Quadratic Models","text":"DifferenceEquations.jl supports second-order perturbation solutions through QuadraticStateSpaceProblem and PrunedQuadraticStateSpaceProblem. These extend the linear state-space model with quadratic terms:\n\nu_n+1 = A_0 + A_1  u_n + u_n^top A_2  u_n + B  w_n+1\n\nwith observation equation\n\nz_n = C_0 + C_1  u_n + u_n^top C_2  u_n + v_n\n\nThis formulation follows Andreasen, Fernandez-Villaverde, and Rubio-Ramirez (2017), \"The Pruned State-Space System for Non-Linear DSGE Models: Theory and Empirical Applications.\"","category":"section"},{"location":"tutorials/quadratic/#Simulating-a-Quadratic-Model","page":"Quadratic Models","title":"Simulating a Quadratic Model","text":"We define the quadratic coefficients. The tensors A_2 and C_2 are 3-dimensional arrays where A_2[i, :, :] gives the matrix for the i-th element of the quadratic form. For a 2-state model, A_2 is a 2×2×2 array: the quadratic contribution to state i is u^top A_2i u. For example, A_2[1,:,:] is the 2×2 matrix whose quadratic form gives the nonlinear correction to the first state element.\n\nusing DifferenceEquations, LinearAlgebra, Random, Plots\n\nA_0 = [-7.824904812740593e-5, 0.0]\nA_1 = [0.95 6.2; 0.0 0.2]\nA_2 = cat([-0.0002 0.0334; 0.0 0.0], [0.034 3.129; 0.0 0.0]; dims = 3)\nB = [0.0; 0.01;;]\nC_0 = [7.8e-5, 0.0]\nC_1 = [0.09 0.67; 1.00 0.00]\nC_2 = cat([-0.00019 0.0026; 0.0 0.0], [0.0026 0.313; 0.0 0.0]; dims = 3)\nD = Diagonal([0.01, 0.01])\nu0 = zeros(2)\nT = 30\n\nRandom.seed!(42)\nprob = QuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, (0, T);\n    C_0, C_1, C_2, observables_noise = D, syms = (:a, :b))\nsol = solve(prob)\n\nThe solution has the same structure as the linear case: sol.u holds the state trajectory, sol.z holds observations, and sol.W holds the noise sequence – all as Vector{Vector}.","category":"section"},{"location":"tutorials/quadratic/#Plotting-and-Ensembles","page":"Quadratic Models","title":"Plotting and Ensembles","text":"The standard plotting recipes work identically:\n\nplot(sol)\n\nEnsemble simulations follow the same SciML interface:\n\nusing DiffEqBase\n\nprob_ens = QuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, (0, T);\n    C_0, C_1, C_2, observables_noise = D, syms = (:a, :b))\nensemble_sol = solve(EnsembleProblem(prob_ens), DirectIteration(), EnsembleThreads();\n    trajectories = 50)\nsumm = EnsembleSummary(ensemble_sol)\nplot(summ)","category":"section"},{"location":"tutorials/quadratic/#Joint-Likelihood","page":"Quadratic Models","title":"Joint Likelihood","text":"When both noise and observables are provided, the solver computes the joint log-likelihood conditioned on the noise realization. As with linear models, observables correspond to z_1 ldots z_T, so we pass sol.z[2:end].\n\nobservables = sol.z[2:end]  # Vector{Vector}, length T\nnoise = sol.W               # Vector{Vector}, length T\n\nprob_lik = QuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, (0, length(observables));\n    C_0, C_1, C_2, observables_noise = D, observables, noise)\nsol_lik = solve(prob_lik)\nsol_lik.logpdf","category":"section"},{"location":"tutorials/quadratic/#Pruned-Quadratic-Models","page":"Quadratic Models","title":"Pruned Quadratic Models","text":"The pruned formulation of Andreasen et al. (2017) prevents explosive dynamics in higher-order perturbation solutions. Instead of applying the quadratic term to the full state, it maintains a separate linear-part state u_f and applies the quadratic form to that:\n\nu_f^n+1 = A_1  u_f^n + B  w_n+1\n\nu_n+1 = A_0 + A_1  u_n + (u_f^n)^top A_2  u_f^n + B  w_n+1\n\nThe PrunedQuadraticStateSpaceProblem takes the same arguments as the unpruned version:\n\nRandom.seed!(42)\nprob_pruned = PrunedQuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, (0, T);\n    C_0, C_1, C_2, observables_noise = D, syms = (:a, :b))\nsol_pruned = solve(prob_pruned)\n\nWith the same noise, we can compare the pruned and unpruned trajectories:\n\nRandom.seed!(100)\nnoise_compare = [randn(1) for _ in 1:T]\n\nsol_unpruned = solve(QuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, (0, T);\n    C_0, C_1, C_2, noise = noise_compare))\nsol_pruned_compare = solve(PrunedQuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, (0, T);\n    C_0, C_1, C_2, noise = noise_compare))\n\nplot(\n    plot([sol_unpruned.u[t][1] for t in eachindex(sol_unpruned.u)], label = \"unpruned\",\n        title = \"State 1\"),\n    plot([sol_pruned_compare.u[t][1] for t in eachindex(sol_pruned_compare.u)],\n        label = \"pruned\", title = \"State 1 (pruned)\"),\n    layout = (1, 2)\n)\n\nThe pruned joint likelihood works the same way:\n\nobs_pruned = sol_pruned.z[2:end]\nnoise_pruned = sol_pruned.W\n\nprob_pruned_lik = PrunedQuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0,\n    (0, length(obs_pruned));\n    C_0, C_1, C_2, observables_noise = D, observables = obs_pruned,\n    noise = noise_pruned)\nsol_pruned_lik = solve(prob_pruned_lik)\nsol_pruned_lik.logpdf","category":"section"},{"location":"tutorials/quadratic/#Differentiating-with-Enzyme","page":"Quadratic Models","title":"Differentiating with Enzyme","text":"The joint likelihood for quadratic models can be differentiated with Enzyme.jl, using the workspace-based init/solve! pattern.\n\nnote: Enzyme.jl required\nThis example requires Enzyme.jl to be installed. The code is shown but not executed during documentation build due to Enzyme's compilation overhead.\n\nusing Enzyme\n\nfunction quad_joint_loglik(A_0, A_1, A_2, B, C_0, C_1, C_2, u0, noise, obs, D,\n        sol, cache)::Float64\n    prob = QuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, (0, length(obs));\n        C_0, C_1, C_2, observables_noise = D, observables = obs, noise)\n    ws = StateSpaceWorkspace(prob, DirectIteration(), sol, cache)\n    return solve!(ws).logpdf\nend\n\n# Pre-allocate workspace\nprob0 = QuadraticStateSpaceProblem(A_0, A_1, A_2, B, u0, (0, length(obs_pruned));\n    C_0, C_1, C_2, observables_noise = D, observables = obs_pruned, noise = noise_pruned)\nws0 = init(prob0, DirectIteration())\n\n# Compute gradient with respect to A_1 (all arguments Duplicated)\ndA_1 = zero(A_1)\nEnzyme.autodiff(Reverse, quad_joint_loglik,\n    Duplicated(copy(A_0), zero(A_0)), Duplicated(copy(A_1), dA_1),\n    Duplicated(copy(A_2), zero(A_2)), Duplicated(copy(B), zero(B)),\n    Duplicated(copy(C_0), zero(C_0)), Duplicated(copy(C_1), zero(C_1)),\n    Duplicated(copy(C_2), zero(C_2)), Duplicated(copy(u0), zero(u0)),\n    Duplicated(deepcopy(noise_pruned), [zeros(size(B, 2)) for _ in noise_pruned]),\n    Duplicated(deepcopy(obs_pruned), [zeros(length(C_0)) for _ in obs_pruned]),\n    Duplicated(copy(D), zero(D)),\n    Duplicated(deepcopy(ws0.output), Enzyme.make_zero(deepcopy(ws0.output))),\n    Duplicated(deepcopy(ws0.cache), Enzyme.make_zero(deepcopy(ws0.cache))))\ndA_1  # gradient of logpdf with respect to A_1","category":"section"},{"location":"getting_started/#Getting-Started","page":"Getting Started","title":"Getting Started","text":"This tutorial walks through the core workflow of DifferenceEquations.jl: defining a linear state-space model, simulating it, and computing likelihoods.","category":"section"},{"location":"getting_started/#Creating-a-Linear-State-Space-Model","page":"Getting Started","title":"Creating a Linear State Space Model","text":"A LinearStateSpaceProblem represents a linear time-invariant state-space model:\n\nu_n+1 = A u_n + B w_n+1 qquad z_n = C u_n + v_n\n\nDefine the model primitives, create a problem, and solve:\n\nusing DifferenceEquations, LinearAlgebra, Random\nA = [0.95 6.2; 0.0 0.2]\nB = [0.0; 0.01;;]  # 2×1 Matrix (Julia's ;; creates a column matrix)\nC = [0.09 0.67; 1.00 0.00]\nu0 = zeros(2)\nT = 10\n\nprob = LinearStateSpaceProblem(A, B, u0, (0, T); C)\nsol = solve(prob)\nsol.u[end]","category":"section"},{"location":"getting_started/#Computing-Likelihood","page":"Getting Started","title":"Computing Likelihood","text":"To compute log-likelihoods, provide observables (a Vector{Vector} of length T) and observables_noise (the observation noise covariance matrix — e.g., Diagonal(d) for diagonal noise or Symmetric(H * H') for a general covariance).\n\nnote: Timing convention\nObservations correspond to z_1 z_2 ldots z_T – that is, the states after the initial condition. Pass T observation vectors for a tspan of (0, T).\n\nFirst, simulate some data to use as observables:\n\nRandom.seed!(123)\nD = Diagonal([0.1, 0.1])  # diagonal observation noise covariance matrix\nprob_sim = LinearStateSpaceProblem(A, B, u0, (0, T); C, observables_noise = D)\nsol_sim = solve(prob_sim)\n\n# Extract observations at times 1..T (skip the initial condition at t=0)\nobservables = sol_sim.z[2:end]\nlength(observables)  # should be T\n\nCompute the joint log-likelihood given fixed noise using DirectIteration:\n\nprob_lik = LinearStateSpaceProblem(A, B, u0, (0, length(observables)); C,\n    observables = observables,\n    observables_noise = D,\n    noise = sol_sim.W)\nsol_lik = solve(prob_lik)\nsol_lik.logpdf  # joint log-likelihood\n\nFor the marginal log-likelihood (integrating out the latent noise), use a KalmanFilter by additionally providing a Gaussian prior on u0:\n\nprob_kf = LinearStateSpaceProblem(A, B, u0, (0, length(observables)); C,\n    observables = observables,\n    observables_noise = D,\n    u0_prior_mean = zeros(2),\n    u0_prior_var = Matrix(1.0I, 2, 2))\nsol_kf = solve(prob_kf)  # KalmanFilter is auto-selected\nsol_kf.logpdf  # marginal log-likelihood","category":"section"},{"location":"getting_started/#DataFrame-Conversion","page":"Getting Started","title":"DataFrame Conversion","text":"Convert the state trajectory to a DataFrame for analysis. Column names come from syms if provided:\n\nusing DataFrames\nprob_df = LinearStateSpaceProblem(A, B, u0, (0, T); C,\n    syms = (:capital, :productivity))\nsol_df = solve(prob_df)\nDataFrame(sol_df)","category":"section"},{"location":"getting_started/#Next-Steps","page":"Getting Started","title":"Next Steps","text":"Linear Simulation – detailed simulation examples, symbolic indexing, fixed noise, and ensemble runs.\nLikelihood & Kalman Filter – marginal and joint likelihood, gradient-based estimation.\nQuadratic Models – second-order perturbation models.\nGeneric Callbacks – user-defined nonlinear transition and observation functions.\nWorkspace API – allocation-free repeated solves for performance-critical loops.\nEnzyme AD – differentiating through solvers and filters with Enzyme.jl.","category":"section"},{"location":"advanced/enzyme_ad/#Enzyme-AD","page":"Enzyme AD","title":"Enzyme AD","text":"DifferenceEquations.jl is fully differentiable with Enzyme.jl in both reverse and forward mode. All examples below use the workspace-based init/solve! pattern with StateSpaceWorkspace, which gives Enzyme the pre-allocated buffers it needs.","category":"section"},{"location":"advanced/enzyme_ad/#The-Core-Pattern","page":"Enzyme AD","title":"The Core Pattern","text":"Every Enzyme example in this package follows the same recipe:\n\nFlat-argument wrapper function. Construct the LinearStateSpaceProblem inside the function from plain matrix/vector arguments. This keeps the Enzyme call site simple and avoids closing over mutable state.\nPre-allocate with init. Call init(prob, alg) once to obtain a workspace whose .output (solution) and .cache fields are correctly sized buffers. Then pass those buffers into the wrapper via StateSpaceWorkspace(prob, alg, sol, cache) followed by solve!(ws).logpdf.\nAll arguments Duplicated. Because every argument flows into the same LinearStateSpaceProblem struct, Enzyme treats the whole struct as active. If even one field is Const while others are Duplicated, Enzyme may silently produce wrong gradients. The safe rule: mark every argument Duplicated.\nZero-initialized shadows for sol/cache. Shadow copies for the solution and cache buffers must be created with Enzyme.make_zero(deepcopy(...)). A plain deepcopy copies the primal values into the shadow, which can produce NaN gradients. make_zero recursively zeroes every numeric field while preserving the nested structure.","category":"section"},{"location":"advanced/enzyme_ad/#Differentiating-Joint-Likelihood","page":"Enzyme AD","title":"Differentiating Joint Likelihood","text":"The joint likelihood conditions on a fixed noise sequence and accumulates the observation log-likelihood along the trajectory via DirectIteration.\n\nusing DifferenceEquations, LinearAlgebra, Enzyme, Random\n\nN, K, M = 2, 1, 2\nA = [0.8 0.1; -0.1 0.7]\nB = [0.1; 0.0;;]\nC = [1.0 0.0; 0.0 1.0]\nD = Diagonal([0.01, 0.01])  # diagonal covariance; use Symmetric(H * H') for non-diagonal\nu0 = zeros(N)\n\nRandom.seed!(42)\nnoise = [randn(K) for _ in 1:5]\nsim = solve(LinearStateSpaceProblem(A, B, u0, (0, 5); C, noise))\nobs = [sim.z[t + 1] + 0.1 * randn(M) for t in 1:5]\n\n# Likelihood function: all matrix args as separate parameters\nfunction di_loglik(A, B, C, u0, noise, obs, R, sol, cache)::Float64\n    prob = LinearStateSpaceProblem(A, B, u0, (0, length(obs));\n        C, observables_noise = R, observables = obs, noise)\n    ws = StateSpaceWorkspace(prob, DirectIteration(), sol, cache)\n    return solve!(ws).logpdf\nend\n\n# Pre-allocate buffers\nprob0 = LinearStateSpaceProblem(A, B, u0, (0, length(obs));\n    C, observables_noise = D, observables = obs, noise)\nws0 = init(prob0, DirectIteration())\n\n# Compute gradient wrt A\ndA = zero(A)\nautodiff(Reverse, di_loglik,\n    Duplicated(copy(A), dA),\n    Duplicated(copy(B), zero(B)),\n    Duplicated(copy(C), zero(C)),\n    Duplicated(copy(u0), zero(u0)),\n    Duplicated(deepcopy(noise), [zeros(K) for _ in noise]),\n    Duplicated(deepcopy(obs), [zeros(M) for _ in obs]),\n    Duplicated(copy(D), zero(D)),\n    Duplicated(deepcopy(ws0.output), Enzyme.make_zero(deepcopy(ws0.output))),\n    Duplicated(deepcopy(ws0.cache), Enzyme.make_zero(deepcopy(ws0.cache))))\ndA  # gradient of logpdf with respect to A","category":"section"},{"location":"advanced/enzyme_ad/#Differentiating-the-Kalman-Filter","page":"Enzyme AD","title":"Differentiating the Kalman Filter","text":"The KalmanFilter computes the marginal log-likelihood by integrating out the latent noise analytically. The same all-Duplicated pattern applies.\n\n# Kalman filter likelihood\nfunction kf_loglik(A, B, C, mu0, Sigma0, R, obs, sol, cache)::Float64\n    prob = LinearStateSpaceProblem(A, B, zeros(eltype(A), size(A,1)), (0, length(obs));\n        C, u0_prior_mean = mu0, u0_prior_var = Sigma0,\n        observables_noise = R, observables = obs)\n    ws = StateSpaceWorkspace(prob, KalmanFilter(), sol, cache)\n    return solve!(ws).logpdf\nend\n\nmu0 = zeros(N)\nSigma0 = Matrix(1.0 * I(N))\nprob_kf = LinearStateSpaceProblem(A, B, zeros(N), (0, length(obs));\n    C, u0_prior_mean = mu0, u0_prior_var = Sigma0,\n    observables_noise = D, observables = obs)\nws_kf = init(prob_kf, KalmanFilter())\n\ndA_kf = zero(A)\nautodiff(Reverse, kf_loglik,\n    Duplicated(copy(A), dA_kf),\n    Duplicated(copy(B), zero(B)),\n    Duplicated(copy(C), zero(C)),\n    Duplicated(copy(mu0), zero(mu0)),\n    Duplicated(copy(Sigma0), zero(Sigma0)),\n    Duplicated(copy(D), zero(D)),\n    Duplicated(deepcopy(obs), [zeros(M) for _ in obs]),\n    Duplicated(deepcopy(ws_kf.output), Enzyme.make_zero(deepcopy(ws_kf.output))),\n    Duplicated(deepcopy(ws_kf.cache), Enzyme.make_zero(deepcopy(ws_kf.cache))))\ndA_kf  # gradient of Kalman logpdf with respect to A","category":"section"},{"location":"advanced/enzyme_ad/#Integration-with-Optimization.jl","page":"Enzyme AD","title":"Integration with Optimization.jl","text":"The differentiable Kalman likelihood composes naturally with Optimization.jl for maximum-likelihood estimation. Because the all-Duplicated requirement cannot be expressed through AutoEnzyme(), we supply an explicit grad function that calls Enzyme.autodiff directly.\n\nusing Optimization, OptimizationOptimJL\n\n# Simulate data from a known model\nRandom.seed!(42)\nT_opt = 200\nB_opt = [0.0; 0.001;;]\nC_opt = [0.09 0.67; 1.00 0.00]\nD_opt = Diagonal([0.01, 0.01])\nprob_data = LinearStateSpaceProblem([0.95 6.2; 0.0 0.2], B_opt, zeros(2), (0, T_opt);\n    C = C_opt, observables_noise = D_opt)\nsol_data = solve(prob_data)\nobs_data = sol_data.z[2:end]\n\n# Pre-allocate Kalman workspace\nmu0_opt = zeros(2)\nSigma0_opt = Matrix(1e-2 * I(2))\nprob_base = LinearStateSpaceProblem([0.95 6.2; 0.0 0.2], B_opt, zeros(2),\n    (0, length(obs_data)); C = C_opt, observables = obs_data,\n    observables_noise = D_opt, u0_prior_mean = mu0_opt, u0_prior_var = Sigma0_opt)\nws_opt = init(prob_base, KalmanFilter())\n\n# Objective and gradient using the flat-argument pattern\nfunction neg_loglik(beta, p)\n    A = [beta[1] 6.2; 0.0 0.2]\n    return -kf_loglik(A, p.B, p.C, p.mu0, p.Sigma0, p.D, p.obs,\n        deepcopy(p.sol), deepcopy(p.cache))\nend\n\nfunction neg_loglik_grad!(g, beta, p)\n    A = [beta[1] 6.2; 0.0 0.2]\n    dA = zero(A)\n    autodiff(Reverse, kf_loglik,\n        Duplicated(A, dA),\n        Duplicated(copy(p.B), zero(p.B)),\n        Duplicated(copy(p.C), zero(p.C)),\n        Duplicated(copy(p.mu0), zero(p.mu0)),\n        Duplicated(copy(p.Sigma0), zero(p.Sigma0)),\n        Duplicated(copy(p.D), zero(p.D)),\n        Duplicated(deepcopy(p.obs), [zeros(2) for _ in p.obs]),\n        Duplicated(deepcopy(p.sol), Enzyme.make_zero(deepcopy(p.sol))),\n        Duplicated(deepcopy(p.cache), Enzyme.make_zero(deepcopy(p.cache))))\n    g[1] = -dA[1, 1]\nend\n\nparams = (; B = B_opt, C = C_opt, D = D_opt, obs = obs_data,\n    mu0 = mu0_opt, Sigma0 = Sigma0_opt, sol = ws_opt.output, cache = ws_opt.cache)\n\noptf = OptimizationFunction(neg_loglik; grad = neg_loglik_grad!)\noptprob = OptimizationProblem(optf, [0.90], params)\noptsol = solve(optprob, LBFGS())\noptsol.u  # estimated beta (true value: 0.95)","category":"section"},{"location":"advanced/enzyme_ad/#Quadratic-and-Generic-Models","page":"Enzyme AD","title":"Quadratic and Generic Models","text":"The same all-Duplicated pattern works for QuadraticStateSpaceProblem, PrunedQuadraticStateSpaceProblem, and StateSpaceProblem. Replace the constructor and add the extra arguments (A_0, A_1, A_2, C_0, C_1, C_2 for quadratic; callback functions for generic) as separate Duplicated parameters. See the Quadratic Models tutorial for an Enzyme example with quadratic problems.","category":"section"},{"location":"advanced/enzyme_ad/#Important-Notes","page":"Enzyme AD","title":"Important Notes","text":"All arguments to the likelihood function that flow into the problem struct must be Duplicated, not Const. This is because Enzyme tracks activity at the struct level.\nShadow copies for sol and cache buffers must be zero-initialized using Enzyme.make_zero(deepcopy(...)). Using plain deepcopy produces NaN gradients.\nThe Optimization.jl integration requires an explicit grad function because AutoEnzyme() cannot directly handle the all-Duplicated requirement. The gradient function calls Enzyme.autodiff manually.\nAvoid calling GC.gc() inside functions differentiated by Enzyme – this can cause segfaults when combined with BenchmarkTools.\nSee the Workspace API page for details on init, solve!, and StateSpaceWorkspace.\nFor small models (N ≤ 5), ForwardDiff AD offers a simpler alternative with comparable performance and no Duplicated bookkeeping.","category":"section"},{"location":"#DifferenceEquations.jl","page":"Home","title":"DifferenceEquations.jl","text":"DifferenceEquations.jl solves initial value problems for deterministic and stochastic difference equations, with differentiable solvers and filters. Automatic differentiation is powered by Enzyme.jl (reverse and forward mode). The package is part of the SciML ecosystem.","category":"section"},{"location":"#Features","page":"Home","title":"Features","text":"Linear, quadratic, and generic state-space models – LinearStateSpaceProblem, QuadraticStateSpaceProblem, PrunedQuadraticStateSpaceProblem, and StateSpaceProblem with user-defined callbacks.\nKalman filter for computing the marginal log-likelihood of linear Gaussian models via KalmanFilter.\nDifferentiable via Enzyme.jl and ForwardDiff.jl – Enzyme reverse/forward mode for all problem sizes; ForwardDiff as a lightweight alternative for small models (N ≤ 5).\nStaticArrays support for small models where heap allocations dominate runtime.\nWorkspace API – StateSpaceWorkspace with init / solve! for allocation-free repeated solves (useful inside AD and tight loops).\nSciML ecosystem integration – EnsembleProblem for Monte Carlo, plot recipes, DataFrame conversion, symbolic indexing, and remake.","category":"section"},{"location":"#Installation","page":"Home","title":"Installation","text":"To install DifferenceEquations.jl, use the Julia package manager:\n\nusing Pkg\nPkg.add(\"DifferenceEquations\")","category":"section"},{"location":"#Quick-Example","page":"Home","title":"Quick Example","text":"using DifferenceEquations, LinearAlgebra\nA = [0.95 0.1; 0.0 0.2]\nB = [0.0; 0.01;;]\nu0 = zeros(2)\nT = 10\nprob = LinearStateSpaceProblem(A, B, u0, (0, T))\nsol = solve(prob)\nsol.u[end]  # final state","category":"section"},{"location":"#Mathematical-Background","page":"Home","title":"Mathematical Background","text":"The general class of discrete-time state-space models supported by this package takes an initial condition u_0 and an evolution equation\n\nu_n+1 = f(u_n p t_n) + g(u_n p t_n) w_n+1\n\nfor transition function f, noise coefficient g, and IID noise shocks w_n+1. The parameter vector p is potentially differentiable.\n\nAn optional observation equation relates the latent state to measured data:\n\nz_n = h(u_n p t_n) + v_n\n\nwhere v_n is observation noise and z_n may have a different dimension from u_n.","category":"section"},{"location":"#Specializations","page":"Home","title":"Specializations","text":"Linear: f(u) = Au, g(u) = B, h(u) = Cu. Solved by DirectIteration or KalmanFilter. See LinearStateSpaceProblem.\nQuadratic: Adds second-order terms u^top A_2 u to both transition and observation. Useful for pruned perturbation solutions of DSGE models. See QuadraticStateSpaceProblem and PrunedQuadraticStateSpaceProblem.\nGeneric: User-supplied transition and observation callbacks. See StateSpaceProblem.\n\nWhen the system is linear, the shocks are Gaussian, and a Gaussian prior is provided, the KalmanFilter computes the exact marginal log-likelihood. For all other cases, DirectIteration iterates the state forward and (optionally) accumulates a joint log-likelihood.\n\nnote: Note\nBoundary value problems and difference-algebraic equations are not in scope.","category":"section"},{"location":"#Contributing","page":"Home","title":"Contributing","text":"Please refer to the SciML ColPrac: Contributor's Guide on Collaborative Practices for Community Packages for guidance on PRs, issues, and other matters relating to contributing to SciML.\nSee the SciML Style Guide for common coding practices and other style decisions.\nThere are a few community forums:\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Slack\nThe #diffeq-bridged and #sciml-bridged channels in the Julia Zulip\nOn the Julia Discourse forums\nSee also SciML Community page","category":"section"},{"location":"#Reproducibility","page":"Home","title":"Reproducibility","text":"<details><summary>The documentation of this SciML package was built using these direct dependencies,</summary>\n\nusing Pkg # hide\nPkg.status() # hide\n\n</details>\n\n<details><summary>and using this machine and Julia version.</summary>\n\nusing InteractiveUtils # hide\nversioninfo() # hide\n\n</details>\n\n<details><summary>A more complete overview of all dependencies and their versions is also provided.</summary>\n\nusing Pkg # hide\nPkg.status(; mode = PKGMODE_MANIFEST) # hide\n\n</details>\n\nusing TOML\nusing Markdown\nversion = TOML.parse(read(\"../../Project.toml\", String))[\"version\"]\nname = TOML.parse(read(\"../../Project.toml\", String))[\"name\"]\nlink_manifest = \"https://github.com/SciML/\" * name * \".jl/tree/gh-pages/v\" * version *\n                \"/assets/Manifest.toml\"\nlink_project = \"https://github.com/SciML/\" * name * \".jl/tree/gh-pages/v\" * version *\n               \"/assets/Project.toml\"\nMarkdown.parse(\"\"\"You can also download the\n[manifest]($link_manifest)\nfile and the\n[project]($link_project)\nfile.\n\"\"\")","category":"section"}]
}
